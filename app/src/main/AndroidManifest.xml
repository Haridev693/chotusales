<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.refresh.chotusalesv1"
    android:versionCode="1"
    android:versionName="1.0">

    <!-- <uses-sdk -->
    <!-- android:minSdkVersion="8" -->
    <!-- android:targetSdkVersion="19" /> -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_LOGS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/chotusalesnewlogo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.mainui.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.SplashScreenActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />
        <activity android:name=".ui.mainui.LoginActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".ui.mainui.DeviceListActivity" />
        <!-- <activity -->
        <!-- android:name="com.refresh.pos.ui.InventoryActivity" -->
        <!-- android:screenOrientation="portrait" /> -->
        <!-- <activity -->
        <!-- android:name="com.refresh.pos.ui.AddProductActivity" -->
        <!-- android:screenOrientation="portrait" /> -->
        <!-- <activity -->
        <!-- android:name="com.refresh.pos.ui.inventory.AddProductLotActivity" -->
        <!-- android:screenOrientation="portrait" /> -->
        <activity
            android:name=".ui.inventory.ProductDetailActivity"
            android:screenOrientation="portrait" />
        <!-- <activity -->
        <!-- android:name="com.refresh.pos.ui.SaleActivity" -->
        <!-- android:screenOrientation="portrait" /> -->
        <!-- <activity -->
        <!-- android:name="com.refresh.pos.ui.StockActivity" -->
        <!-- android:screenOrientation="portrait" /> -->
        <!-- <activity -->
        <!-- android:name="com.refresh.pos.ui.SaleProductCatalog" -->
        <!-- android:screenOrientation="portrait" /> -->
        <!-- <activity -->
        <!-- android:name="com.refresh.pos.ui.sale.SaleLedgerActivity" -->
        <!-- android:screenOrientation="portrait" /> -->
        <activity
            android:name="org.achartengine.GraphicalActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.sale.SaleDetailActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.mainui.DashboardScreen"
            android:theme="@style/AppThemeNoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.mainui.SettingsActivity"
            android:theme="@style/AppThemeNoTitleBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".ui.mainui.TaxSettingActivity"
            android:theme="@style/AppThemeNoTitleBar" />
        <activity
            android:name=".ui.mainui.UserSettingActivity"
            android:theme="@style/AppThemeNoTitleBar" />
        <activity android:name=".ui.mainui.SaveDBtoDrive_Activity" />
        <!-- <activity android:name=".ui.mainui.SaveDBtoDrive_Activity1"/> -->
        <activity android:name=".ShowSalesGraph" />
        <activity android:name=".ui.mainui.exportimport"/>
        <activity android:name=".ui.mainui.PosLoginActivity"/>
    </application>

</manifest>